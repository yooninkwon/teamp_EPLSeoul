<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.tech.EPL.date.mapper.DateMapper">
	<select id="test" resultType="String">
		SELECT DISTNAME FROM DATE_DIST
	</select>

</mapper>